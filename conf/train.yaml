defaults:
  - _self_
  - hydra: train
  - dataset: mik
  - model: ${dataset}
  - trainer: ${dataset}
  - generator: train
  - benchmarking: default
  - paths: [common, train]

job_name: mik:default

seed: 2
cuda: 0
num_workers: 10

pretrained_model_path: null



